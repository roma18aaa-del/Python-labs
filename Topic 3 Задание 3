n = int(input("Введите число: "))
divisors = []
for i in range(2, n // 2 + 1):
    if n % i == 0:
        divisors.append(i)
if divisors:
    print(f"Делители числа {n}: {', '.join(map(str, divisors))}")
else:
    print(f"Число {n} является простым")
